[project]
name = "kb-query-agent"
version = "1.0.0"
description = "Queries the personal knowledge base and synthesizes an answer."
requires-python = ">=3.12"
dependencies = [
    "sentence-transformers>=3.0",
    "sqlite-vec>=0.1.6",
]
