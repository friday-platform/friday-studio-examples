[project]
name = "kb-ingest-agent"
version = "1.0.0"
description = "Ingests a URL or PDF artifact into the personal knowledge base."
requires-python = ">=3.12"
dependencies = [
    "sentence-transformers>=3.0",
    "sqlite-vec>=0.1.6",
    "pymupdf>=1.24",
]
